// manifest.json
{
  "manifest_version": 2,
  "name": "Super Lag Extension",
  "version": "1.0",
  "description": "Runs multiple CPU‑burn and blocking tricks in the background.",
  "background": {
    "scripts": ["background.js"],
    "persistent": true
  },
  "permissions": [
    "<all_urls>"
  ]
}
